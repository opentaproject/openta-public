Cheatsheet

- Ensure the host directory exists: sudo mkdir -p ${DATA_MOUNT}/subdomain-data/db14 
- docker compose up
- docker compose up --pull always --force-recreate
- use docker.ps to the id of the docker container
- docker exec -it XXXXXX bash 2
- py_create_course example1
- go to example1.localhhost:8000
- docker compose down --remove-orphans

